##B2615-神奇幻方
规则驱动的数组模拟编程题
1、语法合规性问题
C++ 标准不支持int A[N][N]这种可变长度数组（VLA），数组大小必须是编译期常量；考试 / OJ 中建议使用定长大数组（如int a[105][105]）或vector。
2、C++ 初始化失效问题
只有固定大小的数组才能用= {0}全零初始化；可变长度数组该写法仅第一个元素为 0，其余是随机垃圾值，会直接导致规则 4 的 “是否未填数” 判断失灵，结果完全错误。